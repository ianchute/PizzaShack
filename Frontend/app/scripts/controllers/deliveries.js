'use strict';

/**
 * @ngdoc function
 * @name frontendApp.controller:DeliveriesCtrl
 * @description
 * # DeliveriesCtrl
 * Controller of the frontendApp
 */
angular.module('frontendApp')
  .controller('DeliveriesCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
